{
  "name": "kiwi-second-brain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check fieldlog/src/worker.js && node --check fieldlog/src/imageSkill.js && node --check cloudflare/src/worker.js && node --check cloudflare/src/imageSkill.js && node --check mcp/src/worker.js && node --check mcp/src/textFold.js && node --check mcp/src/search.js",
    "test": "node --test tests/*.test.js",
    "validate:fieldlog": "npm run build:preview && wrangler deploy --dry-run --config fieldlog/wrangler.jsonc --outdir .wrangler/dry-run-fieldlog",
    "validate:medtec": "wrangler deploy --dry-run --config cloudflare/wrangler.jsonc --outdir .wrangler/dry-run-medtec",
    "validate:mcp": "wrangler deploy --dry-run --config mcp/wrangler.jsonc --outdir .wrangler/dry-run-mcp",
    "validate": "npm run check && npm test && npm run validate:fieldlog && npm run validate:medtec && npm run validate:mcp",
    "build:preview": "node fieldlog/scripts/build-preview-assets.mjs",
    "pretest": "npm run build:preview"
  },
  "devDependencies": {
    "jsdom": "26.1.0",
    "wrangler": "^4.113.0"
  },
  "dependencies": {
    "@aiden0z/pptx-renderer": "1.2.4",
    "@cf-wasm/photon": "^0.4.0"
  }
}
