# MyWiki 每次修改後自我稽核清單

> 目的：避免「修 A 壞 B」。這份清單是永久回歸清單，不是一次性文件。
> 規則：每次修改 MyWiki／Fieldlog 後，都要逐項確認；只要發生過一次「改完東西壞掉」的回歸，就把該問題永久加入本清單，不得刪除，只能補強。

## 0. 基本原則

- [ ] 先確認修改前的既有功能與使用情境，不可只看本次需求。
- [ ] 修改點若碰到錄音、轉錄、附件、編輯器、PWA 快取、版本號、部署流程，必須檢查其相依功能。
- [ ] 不可用 hotfix 藏在不相干檔案中；若臨時 hotfix，後續必須收回正確模組。
- [ ] 任何已發生過的回歸，必須新增成自動測試或至少加入本清單。

## 1. 錄音與背景錄音

- [ ] 開始錄音、停止錄音、10 分鐘自動分段都正常。
- [ ] 分段切換不丟音、不重複啟動 recorder、不產生 0-byte 音檔。
- [ ] 切換瀏覽器分頁時，純錄音不可被 MyWiki 主動停止。
- [ ] 切換到其他 App／頁面 hidden 時，先要求 MediaRecorder 輸出目前 chunk，不可直接結束錄音。
- [ ] iOS 採「最佳努力」：不可因 `isIOSMobile()` 而主動 stop；若系統真的中斷，回前景後要檢查、恢復並留下中斷紀錄。
- [ ] `visibilitychange`、`pagehide`、`freeze`、`pageshow`、`resume` 的行為彼此一致，不可其中一個事件偷偷把錄音結束。
- [ ] 真正離開頁面時要有保護與正常收尾，避免資料遺失。
- [ ] 背景錄音失效是高風險回歸項目：每次改 app.js、生命週期事件、MediaRecorder、PWA 行為後都要重測。

## 2. AI 轉錄與逐字稿

- [ ] 含錄音附件的 Word 文件，頂部必須直接顯示「擷取文字／重新擷取文字」；不可只藏在 `⋯`、舊錄音編輯器或已走不到的相容畫面。
- [ ] 原始 `transcript` 完整文字有保存，可供搜尋與 fallback。
- [ ] `transcript_vtt` 只作時間定位，不可直接把每一個 cue 當一個顯示段落。
- [ ] 中文一字一 cue／同秒大量 cue 時，要合併成正常可讀句段，不能出現 `00:00 表 / 00:00 在 / 00:00 飛...`。
- [ ] VTT 時間格式至少支援 `00.280`、`00:00.280`、`00:00:00.280`。
- [ ] VTT 解析失敗時要退回完整 `transcript`，不能變空白。
- [ ] 多段錄音仍依共同 offset 正確排序。
- [ ] 照片仍能依拍攝時間插入逐字稿附近；修轉錄排版不可破壞照片時間軸。
- [ ] 重新轉錄／重新整理不可覆蓋使用者已人工修改的文件。
- [ ] 單一段轉錄失敗不可拖垮後續所有段落；失敗要可重試並留 history。

## 3. 錄音資料包與附件

- [ ] 錄音卡的 `⋯` 必須開啟資料包管理操作，不可再次導回目前 Word 文件而看似沒有反應。
- [ ] 原始音訊仍可播放、下載／存取。
- [ ] 原始照片不重複、不遺失，附件 id 與時間 offset 正確。
- [ ] 錄音、照片、速記、逐字稿仍屬於同一資料包。
- [ ] 離線或網路不穩時，待補傳附件仍可看見，不可顯示成「沒有附件」。
- [ ] 補傳後 metadata（duration、offset 等）仍存在，轉錄流程可繼續。

## 4. 編輯與顯示

- [ ] 讀取與編輯仍是同一個 Word 式表面，不可又分裂成兩套畫面。
- [ ] 儲存後格式、圖片、錄音附件、超連結不消失。
- [ ] 富文字編輯器的修改不可影響錄音生命週期；錄音邏輯不得藏在 `richtext-editor.js` 之類不相干模組。
- [ ] 手機與桌機都可正常開啟同一筆資料。

## 5. PWA、快取與版本一致性

- [ ] `APP_VERSION`、Worker `UI_VERSION`、`index.html ?v=`、`sw.js CACHE` 必須一致。
- [ ] 修改前端資源後一定要 bump 版本，避免瀏覽器繼續跑舊 app.js。
- [ ] Service Worker 更新後舊快取會失效，不可出現「GitHub 已修好但手機仍是舊版」的假象。
- [ ] 幫助頁顯示版本與實際版本一致。

## 6. 部署確認

- [ ] GitHub commit 成功不等於 production 已部署；不可把「已 commit」說成「已上線」。
- [ ] 必須確認 Cloudflare production 實際部署到預期 commit／版本後，才可宣稱已完成部署。
- [ ] 部署後至少做一次實機 smoke test：開首頁、開資料夾、錄音、切背景、回前景、停止、播放、轉錄、開逐字稿。

## 7. 修改後最小 Smoke Test

每次改 MyWiki，至少快速走一次：

1. 開啟 MyWiki，確認版本號正確且無載入錯誤。
2. 建立／開啟一筆錄音資料包。
3. 錄 20–30 秒，期間切一次分頁／App，再回來。
4. 停止後確認音檔存在且可播放。
5. 執行轉錄，確認不是一字一行，完整內容可讀。
6. 若有錄音中拍照，確認照片仍插在合理時間位置。
7. 編輯逐字稿後儲存重開，確認人工修改未被覆蓋。
8. 確認沒有因本次修改破壞待分類、資料夾瀏覽、附件顯示與 PWA 載入。

## 8. 回歸升級規則

- [ ] 使用者回報「以前可以，改完後壞掉」＝直接視為回歸，不先歸咎瀏覽器或使用方式。
- [ ] 找到根因後，把該情境加入本清單。
- [ ] 能自動化就增加 regression test；不能自動化（例如 iOS 真機背景錄音）就保留在 smoke test。
- [ ] 同一問題第二次再發生時，代表檢查機制不夠，必須補成更具體的測試或部署 gate。

## 已知高頻回歸（永久保留）

- 統一 Word 編輯器後，錄音「擷取文字」只留在舊介面或 `⋯`，實際入口再次消失。
- 錄音卡 `⋯` 錯接到共用 Word 編輯器，點擊後停留原畫面、沒有任何可見動作。
- 背景錄音被 `visibilitychange/pagehide/isIOSMobile` 邏輯主動停止。
- 修錄音生命週期後，分段錄音或回前景恢復失效。
- Whisper VTT 中文被拆成一字一行。
- 修逐字稿排版後，照片時間軸／多段錄音排序被破壞。
- GitHub 已更新但 PWA 快取仍載入舊版。
- `APP_VERSION / UI_VERSION / sw cache / index ?v=` 不一致。
- 只完成 commit 卻誤判為 Cloudflare production 已部署。
- 為了快速 hotfix 把功能塞進不相干檔案，後續再被別的修改意外破壞。
