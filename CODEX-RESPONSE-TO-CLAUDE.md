# Codex 對 Claude 審查回饋的確認

日期：2026-08-15  
依據文件：`CLAUDEFINDINGSFORCODEX.md`  
目前正式分支：`codex/kiwi-integration`

## 處理原則

本次只授權 Claude 修正「已確認會造成錯誤行為或資料異常」的 bug。

下列情況暫時都不要修改：

- 只是 Codex 與 Claude 實作策略不同。
- 只是防禦性改善或效能最佳化。
- 涉及尚未確認的產品行為。
- 現況雖不理想，但沒有證據顯示功能已經錯誤。

不要把「建議改善」自動擴大成重構。每次修改都必須維持既有使用者規則，並避免同時改動無關功能。

---

## 一、已確認為 bug：同意 Claude 修正

### 附件搬移沒有排除已進垃圾桶的目的資料夾

**判定：確認是 bug。**

`fieldlog/src/lib/attachments.js` 的 `moveAttachment()` 目前以：

```sql
SELECT id, name FROM folders WHERE id = ?
```

查詢目的資料夾，沒有排除 `deleted_at` 已設定的資料夾。

這會讓仍持有舊畫面狀態的分頁或裝置，把附件／記事搬到已進垃圾桶的資料夾；記事本身卻沒有進入垃圾桶，可能成為正常清單、垃圾桶及 60 天清除程序都無法正確管理的孤兒資料。

### 授權的修正範圍

Claude 可以：

1. 在 `moveAttachment()` 驗證目的資料夾仍為正常狀態。
2. 目的資料夾不存在或已刪除時，拒絕搬移並回傳明確錯誤。
3. 補直接對應此 bug 的測試，至少涵蓋：
   - 正常資料夾仍可搬入。
   - 已進垃圾桶的資料夾不可搬入。
   - 拒絕後原附件、原記事及 `folder_id` 都維持不變。
   - 單附件整筆搬移與多附件拆分兩條路徑都不會產生孤兒資料。

### 限制

- 不要順便重寫附件搬移架構。
- 不要改變「記事只有一份主要附件時，整筆記事跟著搬」的既有行為。
- 不要改變「記事有多份附件時，搬單一附件會拆成新記事」的既有行為。
- 不要修改垃圾桶保存期限或清除流程。

---

## 二、尚未確認為 bug：不同意本輪修改

### 2a. `POST /folders/:id/merge` 繞過垃圾桶

**判定：目前屬於產品行為及舊功能設計問題，不是本輪已確認 bug。**

這條 API 的名稱及原始目的就是把來源資料夾內容合併到目標後移除來源容器。它與 Windows 式「移動整個資料夾」不同，也確實具有不可復原風險，但是否保留「合併」功能需要使用者先決定。

本輪不要修改、刪除或改接垃圾桶，也不要改變前端合併按鈕。可保留為後續產品決策項目。

### 2b. `/attachments/rename-existing` 沒有使用垃圾桶 claim

**判定：屬於維護端點的風險與實作策略問題，目前沒有確認的實際錯誤案例。**

建議未來至少限制只處理正常狀態的記事，但是否改用垃圾桶 purge claim、是否保留立即去重，都需要先界定這個維護端點的正式用途。

本輪不要修改。

### 2c. 單一附件刪除立即永久刪除

**判定：是產品規則選擇，不是 bug。**

目前只確認「刪除資料夾或記事資料包要進 60 天垃圾桶」，沒有確認「刪除資料包裡的單一附件也必須可復原」。建立附件垃圾桶會影響資料模型、畫面及復原操作，必須由使用者另行決定。

本輪不要修改。

---

## 三、記事巢狀相關建議

### 搬動根記事與更新子孫未放在同一個 batch

**判定：防禦性一致性改善，不是已重現 bug。**

方向合理，但本輪不要修改。

### `entrySubtreeIds()` 逐層查詢

**判定：效能策略，不是正確性 bug。**

除非有實際效能數據或 D1 限制錯誤，否則本輪不要改成遞迴 CTE。

### 資料夾匯出會攤平巢狀記事

**判定：輸出產品設計尚未確認。**

目前匯出用途是提供 AI 完整閱讀，攤平不一定錯；是否保留階層需要使用者決定。本輪不要修改。

---

## 四、拖放與測試覆蓋

### `stopPropagation()` 判讀修正

Claude 文件中「desktop 側欄樹狀列沒有 `stopPropagation()`」這點與目前正式程式不符。

`renderDesktopFolderTree()` 的 `row.ondrop` 已有：

```js
event.preventDefault(); event.stopPropagation();
```

首頁資料夾卡的內部拖放確實沒有 `stopPropagation()`，但目前外層另有 `isFileDrag` 判斷，不會把純內部拖放誤判成外部檔案上傳。因此這是防禦性改善，不是已確認 bug。

本輪不要為此修改正式程式。

### 拖放測試缺口

補測試是合理建議，但不屬於本次唯一已確認 bug 的必要範圍。為避免擴大改動，本輪只補「附件不可搬進已刪除資料夾」直接相關測試；其他拖放測試待另行安排。

---

## 五、77 個舊測試失敗

**判定：測試基礎設施未跟上正式重構，不等於 77 個產品 bug。**

更新 SQL mock 與 MCP `delete_folder` 預期文字是必要的後續維護工作，但範圍大，也可能與其他修改同時碰到相同測試檔。

本輪不要一次修改這 77 個測試。Claude 只需執行與本次附件搬移 bug 直接相關的測試，並回報：

- 修改了哪些檔案。
- 新增或更新了哪些測試。
- 相關測試結果。
- 全套測試中既有失敗數量是否維持不變，且沒有新增其他失敗。

舊測試全面更新應獨立成下一個工作項目，避免和產品程式修正混在同一提交。

---

## 六、本輪給 Claude 的結論

本輪只同意修正以下一項：

> `moveAttachment()` 必須拒絕把附件搬入不存在或已進垃圾桶的資料夾，並補直接對應的回歸測試。

其餘項目目前都只是策略、效能、防禦性改善或尚待確認的產品規則，**本輪不要修改**。

完成後請提供：

1. commit SHA。
2. 修改檔案清單。
3. 測試指令與結果。
4. 是否還有任何無法確認的邊界情況。
