# MyWiki 背景錄音修正報告

- 日期：2026-08-16
- 正式分支：`codex/kiwi-integration`
- 修正版本：v113
- 修正前基準提交：`d07782d`
- 正式網址：<https://fieldlog.gogoyankee.workers.dev/>

## 問題

使用者開始純錄音後，把 MyWiki 切到其他分頁或手機 App，錄音可能被 MyWiki 自己結束。預期行為是錄音維持資料包結構；瀏覽器允許時在背景持續收音，若作業系統暫停麥克風，回到前景後自動檢查及接續。

## 調查結果

1. 最新同步的 12 個 Claude 提交沒有直接修改 `MediaRecorder` 錄音核心，因此不能把問題精確歸因到其中某一筆提交。
2. 錄音生命週期仍有一條會主動停止錄音的路徑：`pagehide` 事件在 `persisted !== true` 時呼叫 `stopAnyActiveCapture()`。
3. `pagehide` 不是可靠的「使用者真正關閉 MyWiki」訊號。手機切換 App、瀏覽器凍結或回收頁面前也可能送出；不同瀏覽器對 `persisted` 的行為並不完全一致。
4. 原有測試反而要求非 bfcache 的 `pagehide` 必須停止錄音，所以這個錯誤行為會通過測試。

## 修正內容

1. `onPageHide()` 一律改走 `onPageHidden()`：
   - 錄影仍依原規則停止，因為背景無法可靠使用鏡頭。
   - 純錄音不停止。
   - 立即呼叫 `MediaRecorder.requestData()`，先保存目前已錄到的資料，降低系統稍後暫停頁面造成的損失。
2. 保留 `beforeunload` 原生離頁警告。使用者真的關閉或離開頁面時，瀏覽器仍會提示；網頁被銷毀後，任何網站都無法繼續使用麥克風。
3. 改寫回歸測試：明確禁止 `onPageHide()` 呼叫 `stopAnyActiveCapture()`。
4. 前端、Worker、HTML 資源與 Service Worker 快取統一升為 v113，避免瀏覽器載到舊版程式。

## 不變的既有功能

- 錄音維持 10 分鐘一段的資料包結構。
- 分段時先啟動新 recorder，再延遲停止舊 recorder，保留重疊以避免空隙。
- 回到前景後仍會驗證是否收到非空音訊資料。
- 音軌 `muted`、`ended` 或 recorder error 時仍會重建麥克風並記錄可能的中斷。
- 照片、附件與錄音仍附加在同一筆錄音紀錄。

## 驗證

- `node --test tests/fieldlog-audio-recording.test.js`：12/12 通過。
- `node --check fieldlog/public/app.js`：通過。
- `git diff --check`：通過。
- 七個 Worker/共用模組的 `node --check`：全部通過。
- 版本、首頁與時區相關測試：版本同步與本次相關項目通過。
- `npm test`：未全綠。正式分支在本修正前即有大量既有 fixture 失敗，主要表現為新增垃圾桶過濾後測試資料被回覆 404，以及舊測試仍期待「收件匣」用語。錄音專項 12 項全部通過；這次不把無關問題混入錄音修正。
- `wrangler deploy --dry-run`：執行環境因套件嘗試連網而攔截，未完成；不是程式編譯錯誤。

## 已知平台限制

這項修正能避免 MyWiki 自己錯誤停止錄音，但不能繞過手機作業系統限制。iOS、Android 或桌面瀏覽器仍可能在鎖屏、記憶體壓力、權限撤銷、節電模式或頁面被真正關閉時暫停/終止麥克風。MyWiki 回到前景後會盡力偵測及接續，並保留先前已切出的錄音片段；不能宣稱所有裝置都能無限背景錄音。

## 建議人工驗收

1. 桌機開始錄音，切到其他分頁 30 秒，再回 MyWiki；確認錄音計時繼續且停止後有音訊。
2. Android 開始錄音，切到其他 App 30 秒，再返回；確認沒有被 MyWiki自動結束。
3. iPhone 重複同一測試；若系統暫停麥克風，確認回前景後出現接續/中斷提示，已錄片段仍存在。
4. 錄音中嘗試在同一頁籤離開網址；確認瀏覽器出現離頁警告。
5. 錄影中切到背景；確認錄影仍依既有規則自動停止並保存。
