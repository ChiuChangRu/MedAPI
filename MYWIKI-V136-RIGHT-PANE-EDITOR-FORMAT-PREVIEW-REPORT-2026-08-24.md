# MyWiki v136：右欄編輯與常用格式預覽修正報告

日期：2026-08-24  
分支：`codex/kiwi-integration`

## 一、修正目標

1. 桌機版統一以右欄作為內容預覽與主要編輯區，不再因按下「編輯」跳回舊展場資料框。
2. 錄音維持完整資料包，不拆散音訊、逐字稿、照片與其他附件。
3. 改善 Excel、CSV 與常用文件格式的預覽結果，並對無法可靠解析的格式提供清楚的後備方式。

## 二、已完成項目

### 右欄預覽與編輯

- 錄音資料包：右欄直接顯示錄音摘要、各段播放器、檔案大小、長度、轉錄狀態、速記與逐字稿。
- 錄音「編輯」：在同一個右欄切換成編輯狀態，可修改名稱、速記及各段逐字稿。
- 一般記事與一般多附件資料包：點選後在右欄顯示內容、欄位與附件；「編輯」也在右欄完成。
- 一般檔案：右欄可修改檔名、檔案速記、醫材分類及解析／索引文字。
- `⋯` 保留重新轉錄、下載、重新命名、移動、刪除及舊資料相容工具；不再是日常主要編輯入口。
- 手機版維持原本清單與對話框操作，避免桌機三欄介面硬塞到小螢幕。

### Excel 與常用格式

| 格式 | v136 右欄行為 | 限制／後備方式 |
|---|---|---|
| XLSX | 自動擷取工作表內容，以可捲動表格呈現 | 不重建公式計算、圖表、顏色、合併儲存格與精確列印排版 |
| CSV | 以表格呈現，支援引號包住逗號及雙引號跳脫 | 預覽最多 500 列、80 欄；複雜跨行引號內容以原檔為準 |
| DOCX | 顯示文件內的可搜尋文字 | 不模擬 Word 的頁面與版面配置 |
| PPTX | 依投影片順序顯示可搜尋文字 | 不重建投影片視覺版面與動畫 |
| PDF | 使用 PDF.js 在右欄逐頁預覽 | 可隨時開啟原檔 |
| 圖片 | 右欄原圖預覽 | — |
| 音訊／影片 | 原生播放器 | — |
| TXT／MD／JSON／XML／LOG | 右欄文字預覽 | 大檔限制預覽前 200,000 字 |
| HTML／HTM | 在 sandbox iframe 中預覽 | 保留隔離，避免頁面程式影響 MyWiki |
| DOC／XLS／PPT | 不嘗試不可靠的舊式二進位解析 | 提示另存成 DOCX／XLSX／PPTX；原檔仍可下載 |
| ODT／ODS／ODP／RTF | 提供原檔入口 | 建議另存成對應的新式 Office 格式 |

### 防呆與相容性

- Excel 解析會依儲存格座標保留空白欄位，避免 C 欄資料被誤顯示到 B 欄。
- 舊版 HTML 富文字記事不會被強制轉成純文字，避免儲存時破壞原排版；名稱與一般欄位仍可在右欄修改，富文字內文暫由 `⋯` 的相容工具處理。
- 原有離線快取、背景同步、資料夾移動與錄音資料包結構均保留。

## 三、驗證結果

- JavaScript 語法檢查：通過。
- Git whitespace／patch 檢查：通過。
- 錄音、右欄工作區、檔案預覽、Excel 文件擷取、移動、背景錄音及自動轉錄相關測試：`62/62` 通過。

## 四、後續建議

若未來需要接近桌面 Office 的完整排版預覽，建議另建受控的文件轉檔服務，將 DOCX／XLSX／PPTX 轉成 PDF 或安全 HTML；不建議直接在 Worker 中重製完整 Office 排版引擎。現階段 v136 以「可讀、可搜尋、可快速確認內容」為優先。
