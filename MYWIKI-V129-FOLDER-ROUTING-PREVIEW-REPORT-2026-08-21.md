# MyWiki v129：資料夾直接歸類與桌機預覽

- 日期：2026-08-21
- 正式分支：`codex/kiwi-integration`
- 前一版本：v128

## 規則變更

先前「外部檔案拖入任何頁面一律進待分類」已被本次需求取代。新規則如下：

1. 在資料夾頁空白處拖入外部檔案：每份檔案直接建立於目前資料夾。
2. 在首頁拖入外部檔案：進入「待分類」。
3. 拖到既有紀錄：仍附加到該紀錄，不改成獨立檔案。
4. 在資料夾工具列上傳、錄音、錄影、拍照、新建紀錄：直接建立於目前資料夾。
5. 錄音維持單一資料包結構，資料包可再移動。
6. 單檔、一般紀錄與錄音資料包都保留移動入口；單檔由「⋯」進入管理後移動。

## 桌機預覽

- 寬度 1000px 以上顯示右側預覽窗格；手機與較窄視窗維持原操作。
- 單擊檔案：右側預覽；雙擊：另開原檔。
- PDF、圖片、HTML、純文字、音訊、影片支援側欄預覽。
- Word／ODT／RTF 顯示既有解析文字；若尚無解析文字，保留開啟原檔。
- 文字檔使用串流讀取，最多顯示前 200,000 字，避免大型檔案一次進入記憶體。

## 安全性

- HTML iframe 使用 `sandbox`，不允許腳本、表單與同源權限。
- `/api/file/:key` 對 HTML 原檔另外回傳 CSP sandbox，並對所有檔案加入 `X-Content-Type-Options: nosniff`。
- 清單載入仍只使用摘要欄位；只有使用者點選某一份 Word 時才讀取該筆完整資料。

## 驗證

- 新增 `tests/fieldlog-folder-routing-preview.test.js`。
- 相關測試 49/49 通過，包含拖放、錄音、預覽、安全性、版本、首頁、效能與時區測試。
- `node --check` 與 `git diff --check` 通過。
- 使用者未提交的 `MYWIKI-AI-LIBRARY-PLAN.md` 未納入本次變更。
