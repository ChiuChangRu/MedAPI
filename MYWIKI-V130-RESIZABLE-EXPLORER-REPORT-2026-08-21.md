# MyWiki v130：可調式三欄檔案總管

- 日期：2026-08-21
- 正式分支：`codex/kiwi-integration`
- 前一版本：v129

## 完成項目

1. 右側單一檔案可拖到左側資料夾樹的任一資料夾。
2. 一般記事與錄音／多附件資料包沿用既有 entry 拖曳格式，也可拖到左側資料夾。
3. 左側目標資料夾在拖曳經過時高亮；拖到原資料夾不做無效搬移。
4. 單檔搬移後顯示「復原」，不以確認對話框打斷拖曳操作。
5. 左側資料夾欄寬沿用既有拖曳調整；中間與右側預覽之間新增第二條調整線。
6. 工具列新增「預覽：開／關」，關閉後中間內容區占滿空間並清空目前預覽。
7. 預覽寬度與開關存在 localStorage；恢復設定及視窗縮放時重新套用最小寬度限制。
8. 鍵盤可在兩條 separator 上使用方向鍵微調欄寬。

## 防呆

- 中間內容區至少保留 360px。
- 預覽至少 280px。
- 1000px 以下不顯示預覽開關與預覽欄，維持手機／窄視窗操作。
- 搬移使用既有 `/attachments/:id/move`，不複製 R2 原檔，不製造第二份檔案內容。

## 驗證

- 擴充 `tests/fieldlog-folder-routing-preview.test.js`，涵蓋左側資料夾放置、復原、開關、調寬及設定保存。
- 使用者未提交的 `MYWIKI-AI-LIBRARY-PLAN.md` 不納入本次提交。
