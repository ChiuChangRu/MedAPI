# MyWiki v135 錄音工作區與 PDF 預覽修正報告

日期：2026-08-24  
正式分支：`codex/kiwi-integration`

## 目的

把錄音資料從舊展場資料框改為日常可使用的專屬工作區，同時保留既有錄音資料包結構；並修正部分舊 PDF 在桌機右側預覽顯示破損的問題。

## 完成項目

### 1. 錄音右側預覽

- 桌機點選錄音資料包後，右側直接顯示：
  - 音訊播放器
  - 錄音建立時間
  - 錄音總長度（資料完整時）
  - 檔案總大小
  - 錄音段數
  - 轉錄狀態
  - 合併逐字稿
  - 速記
- 分段錄音仍逐段提供播放器與下載，不會把資料包拆成多筆獨立紀錄。

### 2. 錄音專用「編輯」

- 編輯畫面只顯示並儲存：
  - 名稱
  - 速記
  - 每段逐字稿
- 不再把展場模板欄位放進日常錄音編輯畫面。
- 人工修改逐字稿後標記為 `manual_edit`，避免背景自動轉錄再次覆蓋；使用者仍可從 `⋯` 明確選擇重新轉錄。

### 3. 錄音 `⋯` 操作

- 提供：
  - 編輯
  - 重新轉錄
  - 重新命名
  - 移動
  - 逐段下載
  - 刪除
- 移動使用 entry 層級，音訊、逐字稿、照片與速記會整包移動。
- 刪除使用 entry 層級，整個資料包移入垃圾桶並保留 60 天。

### 4. 舊展場資料框降為後備介面

- 一般錄音不再開啟舊展場資料框。
- 只有偵測到舊版自訂欄位或舊 HTML 內容時，才在 `舊資料相容工具` 中提供開啟舊版資料框的入口。
- 若歷史資料已缺少錄音附件，仍安全退回舊詳情，不硬套錄音介面。

### 5. PDF 預覽修正

- 桌機右側 PDF 預覽改用既有 PDF.js 直接讀取並繪製，不再依賴瀏覽器 iframe 對 Content-Type 的判斷。
- 增加上一頁／下一頁及頁數顯示。
- `/api/file/:key` 優先採用資料庫保存的 MIME；舊資料為 `application/octet-stream` 時，PDF 會依檔名修正為 `application/pdf`。
- R2 key 改為逐段 URL 編碼，避免含有斜線或特殊字元的舊檔案網址失效。

## 資料結構決策

本次沒有改動錄音資料模型：

- 一筆 `entries` 是一個錄音資料包。
- 音訊、照片及其他檔案仍是該 entry 的 `attachments`。
- 逐字稿仍存放於音訊 attachment 的 `transcript`。
- 速記仍存放於 entry 的 `body`。

因此既有資料、拖曳整包移動、垃圾桶及匯出流程維持相容。

## 測試與驗證

- 新增 `tests/fieldlog-recording-workspace.test.js`，涵蓋六組規格：
  - 專用右側預覽與舊資料後備
  - 播放器／大小／時間／狀態／逐字稿
  - 限定編輯欄位
  - `⋯` 完整操作與整包移動／刪除
  - 人工逐字稿儲存
  - PDF.js 與舊 PDF MIME 修復
- 錄音工作區、既有錄音、背景接續、自動轉錄及資料夾預覽相關測試共 49 項通過。
- `app.js` 與 `worker.js` 均通過 `node --check`。
- `git diff --check` 通過。

## 版本與快取

- 前端／Worker 版本：`v135`
- Service Worker 快取：`fieldlog-v135-recording-workspace`
- `index.html` 的 `style.css`、`app.js`、`pdf-editor.js`、`richtext-editor.js` 查詢版本均同步為 `v135`，部署後會汰換舊快取。
