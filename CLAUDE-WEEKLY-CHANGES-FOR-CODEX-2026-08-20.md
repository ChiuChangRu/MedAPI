# MyWiki／Fieldlog 本週異動摘要（2026-08-13 ～ 08-19，給 Codex）

寫這份文件的目的：長儒要跟你討論系統優化，先把這週 `codex/kiwi-integration`
（production 實際部署追蹤的分支，main 已同步到同一個狀態）上 fieldlog／mcp
的異動整理成一份看得懂前因後果的清單，不是流水帳 commit log。

基準：`aa497bd`（08/15 08:25，上週末狀態）→ `ffda3a8`（08/19 15:14，目前
HEAD）。目前版本號 **v127**，main 與 kiwi-integration 內容逐檔案一致。

測試現況：**384 pass / 77 fail**。這 77 筆是本週從頭到尾都存在的既有失敗
（每一版都有跑，數字沒變過），不是本週任何改動造成的回歸，但也代表這批
沒人清過——放進下面的優化建議。

---

## 一、背景錄音：一場 6 天、15 個版本的除錯 saga（v113→v127 內含）

這是本週份量最大、也最值得跟你講清楚全貌的部分，因為中間繞了不少路，
最後才找到真正的根因。**如果你只看最新的架構，會漏掉「為什麼設計成這樣」
的脈絡**，所以按時間順序寫，不只寫結論。

### 起點：8/14 版本是最後已知可用版

使用者確認 8/14 的背景錄音正常，8/15 之後陸續壞掉。中間 v113～v121（約
8 個版本）都是「使用者實測回報一種新的失敗症狀 → 修 → 上線 → 又冒出下一種
症狀」的循環，每次都只看見症狀的一部分：

- v113：`pagehide` 事件在非 bfcache 情況會主動停止錄音 → 改成不停止
- v114～v116：麥克風恢復流程裡，新／舊音軌的「暫時 muted」被誤判成永久
  失敗 → 加暖機緩衝、加重試
- v117：轉錄安全門檻寫死 7000 但訊息說 10000（順手抓到，見第三節）
- v118：Whisper 對靜音的幻覺輸出（"Thank you" 反覆）沒被過濾，污染逐字稿
  ——這其實是下游症狀，上游還是麥克風真的沒收到聲音
- v119～v121：反覆「重取麥克風」「先釋放舊裝置」「加重試」，每次都解決一個
  症狀、製造下一個症狀（連續「無法接續」→ 修好又變成「214 秒錄音被剁成
  6 段、只錄到 107 秒」）

### 轉折點：v122 架構重寫

把 v118～v121 四次失敗放在一起看，指向同一個根因：**這類機器（Windows
Chrome）的麥克風音軌會變成「殭屍」**——`readyState=live`、`muted=false`、
recorder 照樣吐資料，但樣本內容是數位零。所有靠狀態旗標（`track.muted`、
`recorder.state`）的偵測都會被騙過。而唯一有效的恢復方式（重新
`getUserMedia`）在舊架構下必須重建 `MediaRecorder`，每重建一次就剁一段、
掉幾秒。

新架構把「換麥克風」跟「錄音檔」脫鉤（Web Audio 中繼）：

```
麥克風 stream → MediaStreamSource ─┬→ AnalyserNode（量實際音量，死了就換這個）
                                    └→ MediaStreamDestination → MediaRecorder
                                       （永遠不換，檔案永遠連續）
```

`MediaRecorder` 錄的是 `AudioContext` 的 destination stream，不因任何麥克風
事故換段；偵測改看 `AnalyserNode` 量到的實際樣本（真麥克風連安靜房間都有
底噪，「持續精確全零」才算音軌死了）；換源無縫接軌（先接新、再拔舊）。

### 再一個轉折：v123 發現「換源成功」不等於「真的有聲音」

使用者實測 v122：分段問題確定解決了，但**整段播放沒聲音**。查歷史錄音發現
關鍵證據：8/14 版同一場 3.5 小時錄音，前 131 分鐘逐字稿正常，一次背景中斷
「已自動開新的一段接續」之後，**後面整整 70 分鐘全部是 Whisper 對靜音的
幻覯輸出**。這推翻了「換上全新的 `getUserMedia` 音軌就代表真的收得到聲音」
這個從 v119 到 v122 都在用的假設——比較像作業系統層級把瀏覽器錄音整個
靜音掉，不是任何換源策略能在網頁端繞過的。

v123 先不猜解法，改成老實記錄：換源成功後立即再量一次新音軌訊號，零的話
明講「⚠️ 新音軌可能同樣收不到聲音，不是重取麥克風就能解決的」；全程最高
音量是 0 的話，收尾時 toast 直接警告「這段錄音可能整個是靜音」。

### 收斂：v124～v127

- v124：換源改成逐一驗收實體裝置（避開會跟著系統預設跑、常被 Teams／Line
  搶走的 default／communications 裝置）
- v125：新增「這一段實際編碼出來的位元組率」診斷，跟 AnalyserNode 即時
  讀值分開判斷，避免「訊號表有音量、檔案其實是空的」被漏掉
- v126：**唯一有實測證據支持的假說**——錄影一直是直接錄麥克風原始 track、
  不經過 AudioContext，音訊卻繞經 Web Audio 收音圖的 destination stream。
  兩條路徑拉齊：`MediaRecorder` 一律直接錄 `AUDIO.stream`，`AudioContext`
  只留給音量監測
- v127：跟錄音無關，是同一週發現的獨立 UI bug（見第四節）

### 現況與風險提示

- 全程用「行為測試」取代「原始碼字串比對」——後期版本明確指出早期版本的
  測試方法論有問題：「四次修正都沒修好的原因之一是測試比對的是原始碼裡
  有沒有這段字串，不是錄音有沒有真的收到聲音，所以每加一層防護測試就
  更綠」。這個教訓值得留意：**這類「靠使用者實機回報才發現症狀」的功能，
  字串比對測試的訊號很弱**。
- 目前 v126/v127 這個版本**還沒有使用者長時間實測回報確認完全穩定**——
  saga 本身走了 15 版才收斂，不排除還有沒踩到的邊界情況。系統優化排程時
  這條建議留一點觀察期，不要假設它已經 100% 定案。
- 相關檔案集中在 `fieldlog/public/app.js`（錄音狀態機主體）、
  `tests/fieldlog-audio-recording.test.js`。

---

## 二、語意搜尋（Vectorize）：從缺口重做到上線

之前在別的分支做過一版 Vectorize 語意搜尋，命中率太差沒上線（`app.js`
留有「Vectorize 目前命中率太差」的舊註解）。這次追查發現不是調校問題，
是結構性缺口：

- `triggerEmbedding()` 只掛在附件的轉錄／OCR 寫入點，`entries.body`
  （速記類記事的主要內容）從頭到尾沒被索引
- 長文件整篇只取前 2000 字元就丟去 embedding（模型 `bge-m3` 明明吃得下
  8192 token）

### 重做內容

- schema 補 `vector_id`／`embedding_status`／`embedding_error` 到
  `attachments` 與 `entries`（entries 是新的）
- `EmbeddingWorkflow` + `chunkText()`：記事整篇 embed，附件內容超過 1200
  字才分段（重疊 100 字，上限 20 段）
- 記事 POST/PUT 與 6 個附件 OCR/轉錄寫入點都接上 `triggerEmbedding`；
  `/admin/backfill-embeddings` 一次性補跑舊資料
- `GET /api/search/semantic`：query 向量化 → Vectorize 查詢 → 分流回
  entry/attachment，同附件多段命中去重，過濾 `deleted_at`
- 60 天垃圾桶永久刪除與單一附件硬刪都會清掉對應向量，避免刪除後語意搜尋
  還找得到幽靈結果
- mcp 新增 `search_fieldlog_semantic` 工具，跟既有關鍵字版
  `search_fieldlog` 互補

### 過程中修正的兩個坑

1. **Vectorize index 維度衝突**：一開始以為要另建新 index（舊索引疑似是
   768 維），後來 dashboard 確認既有 `fieldlog-embeddings` 索引其實已經是
   1024 維／cosine，跟 `bge-m3` 相符，改回沿用，不用另建。
2. **繞過 AI 費用保護**（真正花錢的坑）：`EmbeddingWorkflow` 與查詢向量化
   一開始直接呼叫 `env.AI.run()`，繞過專案的 AI Gateway 保護層
   （`budgetedAi()`）。第一次補跑排了 298 筆、每份附件最多再切 20 段，
   吃掉當天免費 Neurons 額度，**害正在進行的錄音即時轉錄被安全門檻擋下**
   ——這是本週背景錄音 saga 裡一度被誤以為是新故障的插曲，實際上是這邊
   的資源搶佔。已改走 `budgetedAi()`，且補跑端點改分批（預設 40 筆，
   `?limit=` 可調，回報 `remaining`）。

### 尚待你評估的部分

- **舊資料補跑目前是手動觸發**（`POST /admin/backfill-embeddings`），
  沒有排程自動跑。第一次已跑過（298 筆），之後新增的舊資料需要人工再打
  一次。
- 之前查證時發現 `fieldlog/src/lib/sync.js`（每日 UTC 18:00 排程同步的
  litdb 等外部來源）寫入 `entries` 是直接進 DB，**繞過 HTTP 層的
  `triggerEmbedding` 呼叫**——同步進來的文獻/專利記事不會自動被索引，
  需要另外接上或依賴 backfill 端點定期跑。這點還沒處理，列在優化清單。
- 語意搜尋目前沒有前端 UI 入口（只有 API 與 MCP 工具），首頁還是關鍵字
  搜尋為主。要不要做成使用者可見的搜尋模式切換，是一個產品面決定。

---

## 三、AI 費用保護：兩個已修正的既有缺陷

- **`AI_AUTO_SAFE_NEURONS` 常數與實際比較值脫鉤**：上次把常數從 7000 調到
  10000 時只改了訊息文字（`預估將超過安全門檻（10,000 Neurons）`），
  比較邏輯裡的數字留在原地（`> 7000`）。實際效果是使用者一直只能用到
  7,000 就被擋，畫面卻說 10,000，而且完全不花錢（10,000 就是免費上限）。
  已改成單一常數來源。
- 見上一節「補跑改分批」——向量化不再跟現場錄音搶額度。

---

## 四、其他本週異動

- **`fieldlog/public/app.js` 首頁搜尋結果框「空白」bug**（v127，最新一筆）：
  `home-search-results` 巢狀在 `home-main-sections` 容器裡，一啟動搜尋，
  `initHomeSearch()` 把整個 `home-main-sections` 隱藏，連同巢狀在裡面的
  搜尋結果框一起被祖先的 `display:none` 蓋過去，畫面整個空白。純看程式碼
  一開始猜錯方向（以為是 CSS specificity），用 Playwright 掛假 API 實際
  模擬打字才抓到根因。修法：把「檢索」區塊拆成跟 `home-main-sections`
  同層的兄弟元素，不再巢狀。**這類「純讀程式碼會猜錯方向」的 UI bug，
  之後懷疑類似症狀時，優先用瀏覽器自動化實測，不要只憑 CSS 規則推理。**
- **關鍵字搜尋納入資料夾名稱／類型比對**：之前只比對標題／內文／欄位，
  記事若歸檔在正確資料夾但內文沒寫關鍵字（例："試模規劃" 歸在
  "專案｜檢體針" 底下卻沒提到"檢體針"三個字）就搜不到。同步修了 fieldlog
  首頁的 `/search` 與 medapi-mcp 的 `search_fieldlog`。
- **畫面時區顯示修正**：後端 `now()` 存 UTC，先前多處畫面直接切 UTC 字串
  顯示，半夜 0～8 點建立的記事會顯示成前一天。新增台北時區轉換函式，
  取代 9 處 raw UTC 顯示點。
- **桌機側欄可拖曳調寬**：`.desktop-explorer-nav` 寬度改成 CSS 變數，
  拖曳把手可調（220–480px），存 localStorage 記得上次寬度。
- **Explorer 資料夾與 60 天垃圾桶**（`e4c7f51`／`076ff72`，8/14）：較大的
  功能新增，`fieldlog/src/lib/trash.js` 新檔，schema 加欄位，含垃圾桶
  自動清除排程。這兩筆是這週開頭的基礎建設，後面的附件搬移 bug（下一節）
  就是建立在這個垃圾桶機制上發現的。
- **統一外部拖入為待分類**（`fa9485c` #55）：外部拖入一律進待分類，待
  分類不計入四層資料夾樹。
- **MCP OAuth 2.1 授權**（`92b9e17` 等 4 筆，8/15）：新檔
  `mcp/src/oauth.js`（472 行），含 cookie 支援、錯誤訊息呈現、ChatGPT
  工具中繼資料揭露。這是這週在 fieldlog 錄音 saga 之外，mcp 這邊做的
  獨立功能，建議你抽空另外過一次這部分——這份文件沒有深入評審 OAuth
  實作本身的安全性，只確認它是本週異動範圍。

---

## 五、附件搬移 bug（已與你對過判讀、獲授權修正）

`moveAttachment()` 查詢目的資料夾時沒有排除 `deleted_at` 已設定的資料夾，
導致附件（進而整筆記事，當這是記事唯一附件時）可以被搬進一個剛被丟進
垃圾桶的資料夾——記事的 `folder_id` 指過去了，但記事自己的 `deleted_at`
不會被設定，變成正常清單看不到、垃圾桶也撈不到、60 天排程更清不到的
孤兒資料。這是本輪**唯一經你授權**修改的項目（`aa497bd`），範圍刻意只鎖
這一個 bug，`CLAUDE-FINDINGS-FOR-CODEX.md` 裡列的其他項目（folder merge
繞過垃圾桶、rename-existing 繞過 purge 鎖定、單一附件刪除沒有 60 天緩衝等）
都還沒動——那份文件應該還是有效的待辦清單，這次沒有取代它。

---

## 六、給系統優化排程的建議清單（按我判斷的優先順序，僅供參考）

1. **`CLAUDE-FINDINGS-FOR-CODEX.md` 裡尚未處理的項目**——上面提到的幾個
   資料夾／垃圾桶相關 bug，這份週報沒有重複列出細節，直接看那份文件。
2. **77 筆長期存在的失敗測試**——本週每一版都跑過、數字從沒變過，代表
   沒人真的去清過。要嘛是測試本身該更新（例如某幾筆已知是「測試用假
   資料庫 SQL 字串比對沒跟上垃圾桶軟刪除條件」），要嘛底下真的藏著沒人
   注意的行為缺陷，值得花一輪專門盤點，不要繼續放著累積。
3. **`sync.js` 排程同步的外部資料不會自動向量化**——見第二節，語意搜尋
   對這批資料是不完整的。
4. **背景錄音 v127 需要更多實測驗證期**才能真的視為收斂，優化排程不要
   假設它已經定案。
5. **MCP OAuth 2.1**（`92b9e17` 等）建議另外抽時間審一次，這份週報沒有
   深入看實作細節。
6. **語意搜尋沒有前端入口**，要不要做成使用者可切換的搜尋模式，是產品
   決定，不是技術債，附帶一提。

---

（本文件為異動摘要，不含程式碼變更；文件本身尚未 commit，供你先過目。）
