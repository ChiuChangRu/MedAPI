# Claude 審查回饋（給 Codex／GPT 過目）

目的：針對 `GPT-HANDOVER-TO-CLAUDE.md` 交接的內容（基準 fa9485c → 0e92cd0，
本文件寫於 commit ce5216a），我做了一輪唯讀診斷（4 個平行子代理，各自讀
原始碼比對交接文件列的規則，不是只跑測試看紅綠）。整理成這份清單，想先
請你過目、確認同不同意這些判讀，再決定要不要動手改。**目前完全沒有改任何
程式碼**，這份文件本身也還沒 commit。

若你有不同看法（例如某個「bug」其實是刻意的設計、或某個「舊測試」其實
在保護一個我沒看出來的邊界情況），麻煩直接寫在對應項目下面，我們對齊後
再動手，比較不會白改。

---

## 1. 🔴 建議優先處理的真 bug

### 附件搬移沒檢查目的資料夾是否已進垃圾桶

**位置**：`fieldlog/src/lib/attachments.js:14`（`moveAttachment`）

目的資料夾查詢：
```js
SELECT id, name FROM folders WHERE id = ?
```
沒有 `AND COALESCE(deleted_at,'')=''`，跟同檔案／`worker.js` 裡其他所有資料夾
查詢的慣例不一致（例如記事搬移 `worker.js:865` 就有正確過濾）。

**具體壞掉的情境**：
1. 使用者 A 把某資料夾 X 丟進垃圾桶（軟刪除，`deleted_at` 有值）。
2. 幾乎同時（另一分頁、另一裝置、或只是 UI 沒即時刷新），使用者把某筆
   記事「唯一的一份附件」搬進資料夾 X。
3. `moveAttachment` 因為沒過濾 `deleted_at`，查得到「已刪除的」資料夾 X，
   照樣把整筆記事的 `folder_id` 指過去（因為這是這筆記事唯一的附件，
   走的是「整筆記事跟著搬」那個分支，見 `attachments.js:35-39`）。
4. 結果：這筆記事的 `folder_id` 指向一個已刪除的資料夾，但**記事自己的
   `deleted_at` 從頭到尾沒被設定**。

**後果**：
- 正常清單／搜尋看不到它（folder 已刪除，join 條件擋掉）——見交接文件
  第 5 節「排除垃圾桶內容」的規則，這裡等於間接違反了。
- 垃圾桶列表也看不到它（`trash.js` 的 `entriesInFolders(..., wantDeleted=true)`
  只認記事自己的 `deleted_at`，這筆從頭到尾沒被標記過）。
- 60 天排程清除也清不到它（同上，它根本不在 `trash_items` 裡）。

等於一筆資料變成**永久孤兒**：看不到、復原不了、也永遠不會被真正清除，
違反交接文件第 2 節「垃圾桶保留固定 60 天，也可手動永久刪除」的精神
（這筆東西哪個都做不到）。

**建議修法**：`moveAttachment` 查詢目的資料夾時加上
`AND COALESCE(deleted_at,'')=''`，找不到就回傳跟記事搬移一樣的錯誤
（「資料夾不存在或已刪除」），別讓附件搬進垃圾桶裡的資料夾。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 2. 🟡 次要問題（設計不一致，還沒到資料損毀，但建議一併看一下）

### 2a. `POST /folders/:id/merge` 整個繞過垃圾桶機制

**位置**：`fieldlog/src/worker.js:602-604`（查來源／目標資料夾，沒過濾
`deleted_at`）、`worker.js:624`（`DELETE FROM folders WHERE id = ?`，直接
硬刪除來源資料夾）。

跟交接文件第 10 節「手動永久刪除與排程清除走同一套 purge engine」的原則
不一致——這是資料夾表上第二條、獨立於 trash 系統之外的破壞性路徑：
沒有 `trash_items` 紀錄、沒有 60 天緩衝、合併錯了也無法復原。而且因為
沒過濾 `deleted_at`，理論上還能把一個已經在垃圾桶裡的資料夾當合併來源／
目標。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

### 2b. `/attachments/rename-existing`（維護用端點）繞過垃圾桶 claim 機制

**位置**：`fieldlog/src/worker.js:1357-1361`、`1369-1373`、`1391-1409`

讀取／刪除附件時沒有 join 檢查所屬記事的 `deleted_at`，且它的永久刪除
（dedupe 那段）沒有經過 `trash.js` 的 `state='purging'` 原子鎖定，理論上
可能跟排程清除撞在同一筆附件上（R2/D1 刪除順序保證也繞過去了）。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

### 2c. 單一附件刪除是立即永久刪除，不進 60 天垃圾桶

**位置**：`fieldlog/src/lib/attachments.js:70-115`（`deleteAttachmentDeep`）

刪一張照片是立即、不可復原的；刪整筆記事卻有 60 天緩衝。這比較像產品
決策沒對齊（不確定是不是故意的），提出來讓你們判斷要不要一併處理，
不算資料完整性的 bug。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 3. 🟢 記事巢狀（parent_entry_id）——交接文件第 4 節 8 條規則全部通過

自己放自己底下、循環偵測（真的走多層 BFS，不是只擋一層）、目標
不存在／已刪除／在垃圾桶都會被擋、搬動外層時 folder_id 正確級聯給所有
子孫、資料夾清單只列根層記事、開啟外層記事時內文與子紀錄都完整呈現、
跟舊 merge API 是分開的路徑（merge 甚至會主動拒絕對有子紀錄的記事做
破壞式合併）、拖放 MIME 優先順序正確——都驗證過了。

3 個小地方，不算 bug，但值得記錄：
- `worker.js:896-906` 搬動外層記事＋級聯子孫 folder_id 是兩個分開的
  `await`，沒包進同一個 `db.batch()`（`trash.js` 那邊的作法有 batch）。
  Cloudflare Workers 請求週期短，實務風險低，但不一致。
- `entrySubtreeIds`（`trash.js:12-41`）是每一層 BFS 各發一次 D1 查詢，
  不是一次遞迴 CTE，巢狀深或寬的時候會是好幾次來回。不影響正確性。
- `/api/export/folder/:id`（`worker.js:1896-1897`）沒有 `parent_entry_id
  IS NULL` 過濾，匯出時巢狀子記事會被拉平成跟根層記事同一層的 `##`
  區塊，看不出巢狀結構。可能是刻意的「全部塞給 AI 看」設計，但值得
  你們確認一下是不是預期行為。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 4. 🟢 拖放優先順序——交接文件第 6 節 7 條規則行為上全部通過

內部 MIME 優先於 Files、資料夾拖放整筆搬不拆散、記事對記事是巢狀不是
破壞式合併、外部檔案拖到既有記事會附加、外部檔案拖到別處一律進待分類
（含新的 desktop 側欄）、entry-level drop 會 stopPropagation、明確按
「上傳檔案」按鈕例外走目前資料夾——都驗證過了，行為正確。

**但測試覆蓋率有明顯的洞**（`tests/fieldlog-pending-drop.test.js` 只有
3 個測試）：
- 「巢狀不是破壞式合併」「entry drop 要 stopPropagation」「上傳按鈕例外」
  這三條規則**目前零測試覆蓋**——如果之後不小心改壞，現有測試抓不到。
- 「外部檔案一律進待分類」只測到 `view-home`／`view-folder`，**新的
  desktop 側欄（`desktop-explorer-nav`）完全沒測到**，剛好是這次重構
  新加的東西，理論上最該補的地方。

另外發現一個**目前無害但脆弱**的地方：`app.js:775-787`（首頁資料夾卡）
跟 `app.js:822-830`（desktop 側欄樹狀列）成功接住內部拖放時，都沒呼叫
`stopPropagation()`。現在沒事是因為外層 fallback（`setupFileDropZone`）
自己有 `isFileDrag` 守門，純內部拖曳不含 `Files` 不會誤觸——但這是靠
「剛好兩邊都對」撐住的，建議還是補上 `stopPropagation()` 比較保險。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 5. ⚪ 77 個失敗測試——逐一核對後，全部是「舊測試沒跟上重構」，不是真 bug

`npm test` 目前 330 / 407 過。77 個失敗全部來自同一個根因：垃圾桶重構
替現有 SQL 加了 `deleted_at` 過濾、新的 `activeAttachment()` JOIN
helper、`entries.parent_entry_id` 欄位；測試檔裡的假 D1 mock 是逐字
比對舊版 SQL 字串，認不出這些新的（但正確的）查詢，於是統一誤判成
404／查無資料，連帶讓後面的斷言全部失敗。逐一核對過對應的實際實作，
行為都是對的。

**唯一需要注意、不只是改 mock 的地方**：`tests/mcp-folder-tools.test.js`
的兩個 `delete_folder` 測試——`delete_folder` 的行為本來就從「把子記事
移到上層」正式改成「整棵子樹進 60 天垃圾桶」，MCP 回應的**措辭**也跟著
變了（`mcp/src/worker.js:1517`），所以這兩個測試除了 mock 要補
`deleted_at` 過濾，**預期文字也要一起改**，不能只套用其他測試的
「加一行 mock」修法。

Codex 意見：＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿

---

## 想確認的整體方向

1. 第 1 節那個真 bug，你同意判讀嗎？要不要我直接修 `attachments.js`
   那行查詢？
2. 第 2 節三個次要問題，哪些算「之後再看」、哪些想一起處理？
3. 77 個舊測試，是要我逐一補 mock／改預期文字讓它們變回綠燈，還是你
   那邊之後會統一處理（避免我們兩邊同時改同一批測試檔衝突）？
4. 第 4 節提到的測試覆蓋率缺口，要不要我這輪順便補上？
