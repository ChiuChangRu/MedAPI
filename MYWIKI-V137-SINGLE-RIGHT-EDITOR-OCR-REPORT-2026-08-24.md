# MyWiki v137：桌機單一右欄編輯與文字擷取修正報告

日期：2026-08-24  
正式分支：`codex/kiwi-integration`  
正式站：`https://fieldlog.gogoyankee.workers.dev/`

## 問題判定

使用者畫面已經出現 v136 的右欄 PDF 預覽與右欄基本編輯，代表前端版本有更新；並非因為未合併到 `main`。真正原因是 v136 為了相容舊資料，仍讓檔案與錄音的 `⋯` 開啟舊版展場資料框，形成兩套編輯模式。

目前 MyWiki 正式部署工作流程監聽 `codex/kiwi-integration` 的 `fieldlog/**` 變更，因此本次不需要合併 `main` 才能部署。

## v137 修正內容

1. 桌機資料夾工作區只使用右欄預覽、編輯與管理。
2. 檔案列的 `⋯`、右欄預覽的 `⋯` 與「編輯」均進入同一個右欄編輯器，不再開啟舊展場資料框。
3. 右欄檔案編輯器整合：
   - 檔案名稱
   - 附屬記事
   - 醫療器材分類
   - 擷取／索引文字
   - 擷取或重新擷取文字
   - 複製索引文字
   - PDF 逐頁深度擷取
   - 整理中文檔名
   - 移動、唯讀分享、PDF 塗鴉及刪除
4. PDF 逐頁擷取完成後仍停留並更新右欄，不再跳回舊介面。
5. 錄音的 `⋯` 在桌機直接進入右欄編輯器；右欄可：
   - 修改名稱、速記與每段逐字稿
   - 重新轉錄全部錄音段
   - 下載各段音訊
   - 整包移動或移入垃圾桶
6. 錄音仍維持資料包結構，音訊、逐字稿及照片不會分散。
7. 手機保留既有相容介面，避免小螢幕操作退化；「桌機只用右欄」不受影響。
8. 舊版 HTML 富文字資料不再引導使用者開第二套編輯框；為保護原排版，舊內文暫時唯讀，名稱與欄位仍在右欄修改。

## 格式支援檢查

- PDF：PDF.js 右欄逐頁預覽；可擷取文字與逐頁深度擷取。
- 圖片：右欄圖片預覽；可擷取圖片文字。
- DOCX／PPTX：右欄顯示擷取文字。
- XLSX：把工作表及儲存格擷取成右欄表格。
- CSV／TXT／MD／JSON／XML：直接在右欄預覽。
- HTML：使用 sandbox iframe 預覽。
- 舊 DOC／XLS／PPT、ODT／ODS／ODP／RTF：維持清楚的格式轉換提示，不假裝可可靠預覽。

## 驗證

- JavaScript 語法檢查：通過。
- `git diff --check`：通過。
- 文件擷取、Excel、PDF、資料夾搬移、錄音、背景錄音、自動轉錄與右欄工作區回歸測試：63/63 通過。
- 版本號已同步更新為 v137：`app.js`、Worker、HTML 快取參數與 Service Worker 快取名稱一致。

## 後續原則

舊版展場資料框只保留作為手機或舊資料相容後備，不再作為桌機資料夾工作區的日常入口。後續新增的檔案／錄音功能應優先放入右欄，避免再次產生兩套編輯流程。
